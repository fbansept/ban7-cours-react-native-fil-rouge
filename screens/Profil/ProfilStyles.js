import { StyleSheet } from "react-native";
import theme from "../../theme";

export default function ProfilStyles() {
  const colors = theme();
  const styles = StyleSheet.create({});
  return styles;
}
